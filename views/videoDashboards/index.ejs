<%- include('../partials/header'); -%>
<%- include('../partials/flash'); -%>

<% if (dashboards.length) { %>

<h3 class="mb-4">Meus dashboards de vídeo</h3>

<div class="row row-cols-1 row-cols-md-2 g-4">
    <% for (let dashboard of dashboards) { %>

    <div class="col">
        <div class="card">
            <!-- <img src="../imgs/placeholder.webp" class="card-img-top" alt="..."> -->
            <div class="card-body">
                <h5 class="card-title"><%= dashboard.title %></h5>
                <p class="card-text">ID da vídeo: <%= dashboard.videoId %></p>
                <div class="btn-group" role="group" aria-label="Basic outlined example">
                    <a type="button" class="btn btn-outline-primary"
                        href="https://www.youtube.com/watch?v=<%= dashboard.videoId %>" target="_blank">Acessar
                        no YouTube</a>
                    <a type="button" class="btn btn-outline-primary"
                        href="/videoDashboards/<%= dashboard._id %>">Editar</a>
                    <a type="button" class="btn btn-outline-primary" href="/videoDashboards/<%= dashboard._id %>/dash"
                        target="_blank">Visualizar</a>
                </div>
            </div>
        </div>
    </div>

    <% } %>

</div>

<% } else { %>
Parece que você ainda não tem nenhum dashboard de vídeo, gostaria de criar um? <a href="/videoDashboards/new">Criar
    dashboard de vídeo.</a>
<% } %>

<%- include('../partials/footer'); -%>