<%- include('../partials/header'); -%>
<%- include('../partials/flash'); -%>

<% if (dashboards.length) { %>

<h3 class="mb-4">Meus dashboards de playlist</h3>

<div class="row row-cols-1 row-cols-md-2 g-4">
    <% for (let dashboard of dashboards) { %>

    <div class="col d-flex align-items-stretch">
        <div class="card w-100">
            <!-- <img src="../imgs/placeholder.webp" class="card-img-top" alt="..."> -->
            <div class="card-body d-flex flex-column justify-content-between">
                <h5 class="card-title mb-3">
                    <%= dashboard.title %>
                </h5>
                <div class="btn-group" role="group" aria-label="Basic outlined example">
                    <a type="button" class="btn btn-outline-primary"
                        href="https://www.youtube.com/playlist?list=<%= dashboard.playlistId %>" target="_blank">Acessar
                        no YouTube</a>
                    <a type="button" class="btn btn-outline-primary"
                        href="/playlistDashboards/<%= dashboard._id %>">Editar</a>
                    <a type="button" class="btn btn-outline-primary"
                        href="/playlistDashboards/<%= dashboard._id %>/dash/0" target="_blank">Visualizar</a>
                </div>
            </div>
        </div>
    </div>

    <% } %>

</div>

<% } else { %>
Parece que você ainda não tem nenhum dashboard de playlist, gostaria de criar um? <a
    href="/playlistDashboards/new">Criar
    dashboard de playlist.</a>
<% } %>

<%- include('../partials/footer'); -%>