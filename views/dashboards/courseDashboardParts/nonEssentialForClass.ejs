<div class="row mb-2" id="forCNEvideoContent">
    <div class="col-12 col-lg-4 px-1">
        <div class="container-fluid">
            <!-- Video 0 chapters -->
            <% if (chaptersData.forCNEchaptersData0 && chaptersData.forCNEchaptersData0.length) { %>
            <div class="row dropdown mb-1" id="forCNEchaptersDropdown0">
                <button class="btn btn-dark dropdown-toggle w-100" type="button" data-bs-toggle="dropdown"
                    aria-expanded="false">
                    Capítulos do vídeo
                </button>
                <ul class="dropdown-menu" aria-labelledby="capitulos-video" id="forCNEchaptersList0">
                    <% for (let i = 0; i < chaptersData.forCNEchaptersData0.length; i++) { %>
                    <li class="dropdown-item btn btn-link text-wrap"
                        id="<%= 3600*chaptersData.forCNEchaptersData0[i].timestamp[0] + 60*chaptersData.forCNEchaptersData0[i].timestamp[1] + chaptersData.forCNEchaptersData0[i].timestamp[2] %>">
                        <%= chaptersData.forCNEchaptersData0[i].title %>
                    </li>
                    <% } %>
                </ul>
            </div>
            <% } else { %>
            <p id="forCNEchaptersDropdown0">Este vídeo não está separado em capítulos.</p>
            <% } %>

            <!-- Video 1 chapters -->
            <% if (chaptersData.forCNEchaptersData1 && chaptersData.forCNEchaptersData1.length) { %>
            <div class="row dropdown mb-1 d-none" id="forCNEchaptersDropdown1">
                <button class="btn btn-dark dropdown-toggle w-100" type="button" data-bs-toggle="dropdown"
                    aria-expanded="false">
                    Capítulos do vídeo
                </button>
                <ul class="dropdown-menu" aria-labelledby="capitulos-video" id="forCNEchaptersList1">
                    <% for (let i = 0; i < chaptersData.forCNEchaptersData1.length; i++) { %>
                    <li class="dropdown-item btn btn-link text-wrap"
                        id="<%= 3600*chaptersData.forCNEchaptersData1[i].timestamp[0] + 60*chaptersData.forCNEchaptersData1[i].timestamp[1] + chaptersData.forCNEchaptersData1[i].timestamp[2] %>">
                        <%= chaptersData.forCNEchaptersData1[i].title %>
                    </li>
                    <% } %>
                </ul>
            </div>
            <% } else { %>
            <p class="d-none" id="forCNEchaptersDropdown1">Este vídeo não está separado em capítulos.</p>
            <% } %>

            <!-- Video 2 chapters -->
            <% if (chaptersData.forCNEchaptersData2 && chaptersData.forCNEchaptersData2.length) { %>
            <div class="row dropdown mb-1 d-none" id="forCNEchaptersDropdown2">
                <button class="btn btn-dark dropdown-toggle w-100" type="button" data-bs-toggle="dropdown"
                    aria-expanded="false">
                    Capítulos do vídeo
                </button>
                <ul class="dropdown-menu" aria-labelledby="capitulos-video" id="forCNEchaptersList2">
                    <% for (let i = 0; i < chaptersData.forCNEchaptersData2.length; i++) { %>
                    <li class="dropdown-item btn btn-link text-wrap"
                        id="<%= 3600*chaptersData.forCNEchaptersData2[i].timestamp[0] + 60*chaptersData.forCNEchaptersData2[i].timestamp[1] + chaptersData.forCNEchaptersData2[i].timestamp[2] %>">
                        <%= chaptersData.forCNEchaptersData2[i].title %>
                    </li>
                    <% } %>
                </ul>
            </div>
            <% } else { %>
            <p class="d-none" id="forCNEchaptersDropdown2">Este vídeo não está separado em capítulos.</p>
            <% } %>

            <div class="row dropdown mb-1">
                <button class="btn btn-dark dropdown-toggle w-100" type="button" data-bs-toggle="dropdown"
                    aria-expanded="false">
                    Vídeos
                </button>
                <ul class="dropdown-menu" aria-labelledby="para-aula-nao-essencial-videos">
                    <% for (let i = 0; i < dashboard.classes[classIndex].forClassMaterial.nonEssential.video.length; i++) { %>
                    <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.video[i] !== '') { %>
                    <li><button class="dropdown-item" id="forCNEvideo<%= i %>"
                            value="https://www.youtube.com/embed/<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.video[i] %>?rel=0">
                            Vídeo <%= i + 1 %>
                        </button></li>
                    <% } %>
                    <% } %>
                </ul>
            </div>
            <!-- Slides -->
            <div class="row dropdown mb-1">
                <button class="btn btn-dark dropdown-toggle w-100" type="button" id="para-aula-nao-essencial-slides"
                    data-bs-toggle="dropdown" aria-expanded="false">
                    Slides
                </button>
                <ul class="dropdown-menu" aria-labelledby="para-aula-nao-essencial-slides">
                    <% for (let i = 0; i < dashboard.classes[classIndex].forClassMaterial.nonEssential.slide.length; i++) { %>
                    <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.slide[i] !== '') { %>
                    <li><a target="_blank" class="dropdown-item"
                            href="<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.slide[i] %>">
                            Slide <%= i + 1 %>
                        </a></li>
                    <% } %>
                    <% } %>
                </ul>
            </div>
            <!-- Exercises lists -->
            <div class="row dropdown mb-1">
                <button class="btn btn-dark dropdown-toggle w-100" type="button"
                    id="para-aula-nao-essencial-listas-de-exercicios" data-bs-toggle="dropdown" aria-expanded="false">
                    Listas de exercícios
                </button>
                <ul class="dropdown-menu" aria-labelledby="para-aula-nao-essencial-listas-de-exercicios">
                    <% for (let i = 0; i < dashboard.classes[classIndex].forClassMaterial.nonEssential.exerciseList.length; i++) { %>
                    <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.exerciseList[i] !== '') { %>
                    <li><a target="_blank" class="dropdown-item"
                            href="<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.exerciseList[i] %>">
                            Lista <%= i + 1 %>
                        </a></li>
                    <% } %>
                    <% } %>
                </ul>
            </div>
            <!-- Readings -->
            <div class="row dropdown mb-1">
                <button class="btn btn-dark dropdown-toggle w-100" type="button" id="para-aula-nao-essencial-leituras"
                    data-bs-toggle="dropdown" aria-expanded="false">
                    Leituras
                </button>
                <ul class="dropdown-menu" aria-labelledby="para-aula-nao-essencial-leituras">
                    <% for (let i = 0; i < dashboard.classes[classIndex].forClassMaterial.nonEssential.reading.length; i++) { %>
                    <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.reading[i] !== '') { %>
                    <li><a target="_blank" class="dropdown-item"
                            href="https://www.youtube.com/watch?v=<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.reading[i] %>">
                            Leitura <%= i + 1 %>
                        </a></li>
                    <% } %>
                    <% } %>
                </ul>
            </div>
            <!-- Quizzes -->
            <div class="row dropdown mb-1">
                <button class="btn btn-dark dropdown-toggle w-100" type="button"
                    id="para-aula-nao-essencial-questionarios" data-bs-toggle="dropdown" aria-expanded="false">
                    Quizzes
                </button>
                <ul class="dropdown-menu" aria-labelledby="para-aula-nao-essencial-questionarios">
                    <% for (let i = 0; i < dashboard.classes[classIndex].forClassMaterial.nonEssential.quizz.length; i++) { %>
                    <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.quizz[i] !== '') { %>
                    <li><a target="_blank" class="dropdown-item"
                            href="<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.quizz[i] %>">
                            Quizz <%= i + 1 %>
                        </a></li>
                    <% } %>
                    <% } %>
                </ul>
            </div>
            <!-- External links -->
            <div class="row dropdown">
                <button class="btn btn-dark dropdown-toggle w-100" type="button"
                    id="para-aula-nao-essencial-links-externos" data-bs-toggle="dropdown" aria-expanded="false">
                    Links externos
                </button>
                <ul class="dropdown-menu" aria-labelledby="para-aula-nao-essencial-links-externos">
                    <% for (let i = 0; i < dashboard.classes[classIndex].forClassMaterial.nonEssential.externalLink.length; i++) { %>
                    <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.externalLink[i] !== '') { %>
                    <li><a target="_blank" class="dropdown-item"
                            href="<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.externalLink[i] %>">
                            Link <%= i + 1 %>
                        </a></li>
                    <% } %>
                    <% } %>
                </ul>
            </div>
        </div>
    </div>
    <div class="col-12 col-lg-8 px-3 py-2 p-lg-0">
        <% if (dashboard.classes[classIndex].forClassMaterial.nonEssential.video[0] !== '') { %>
        <div class="ratio ratio-16x9">
            <iframe
                src="https://www.youtube.com/embed/<%= dashboard.classes[classIndex].forClassMaterial.nonEssential.video[0] %>?rel=0"
                title="YouTube video" id="forCNEvideo" allowfullscreen></iframe>
        </div>
        <% } else { %>
        <p class="text-center">Esta parte da aula não possui vídeos.</p>
        <% } %>
    </div>
</div>