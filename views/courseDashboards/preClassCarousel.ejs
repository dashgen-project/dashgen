<div class="p-3 rounded mb-3 carousel-block-1">
    <div class="container">
        <div class="row">
            <div class="col-12 col-lg-6">
                <div id="carousel1" class="carousel slide mb-3" data-ride="carousel" data-bs-interval="false">
                    <div class="carousel-inner px-5 py-3 bg-light rounded">
                        <h3 class="text-dark text-center text-decoration-underline">Materiais Essenciais</h3>
                        <div class="carousel-item active px-5">
                            <h4 class="">Vídeos</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_E_video<%= i %>" class="form-label  fs-5">Vídeo <%= i + 1 %></label>
                            <select class="form-select mb-1" aria-label="Select video"
                                name="thisClass[preClassMaterial][essential][video][<%= i %>]"
                                id="PreC_E_video<%= i %>">
                                <% if (dashboard.classes[classNum]) { %>
                                <% if (dashboard.classes[classNum].preClassMaterial.essential.video[i]) { %>
                                <option value="">Nenhum</option>
                                <% for (let video of videos) { %>
                                <% if
                                                    (video.videoId===dashboard.classes[classNum].preClassMaterial.essential.video[i]){
                                                    %>
                                <option selected value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } else { %>
                                <option value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } %>
                                <% } %>
                                <% } else { %>
                                <option selected value="">Nenhum</option>
                                <% for (let video of videos) { %>
                                <option value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } %>
                                <% } %>
                                <% } else { %>
                                <option selected value="">Nenhum
                                </option>
                                <% for (let video of videos) { %>
                                <option value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } %>
                                <% } %>
                            </select>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Slides</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_E_slide<%= i %>" class="form-label  fs-5">Slide <%= i + 1 %></label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][slide][<%= i %>]" id="PreC_E_slide<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.essential.slide[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][slide][<%= i %>]"
                                id="PreC_E_slide<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Listas de exercícios</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_E_lista<%= i %>" class="form-label  fs-5">Lista <%= i + 1 %></label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][exerciseList][<%= i %>]"
                                id="PreC_E_lista<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.essential.exerciseList[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][exerciseList][<%= i %>]"
                                id="PreC_E_lista<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Leituras</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_E_leitura<%= i %>" class="form-label  fs-5">Leitura <%= i + 1 %>
                            </label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][reading][<%= i %>]"
                                id="PreC_E_leitura<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.essential.reading[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][reading][<%= i %>]"
                                id="PreC_E_leitura<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Quizzes</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_E_quizz<%= i %>" class="form-label  fs-5">Quizz <%= i + 1 %></label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][quizz][<%= i %>]" id="PreC_E_quizz<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.essential.quizz[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][quizz][<%= i %>]"
                                id="PreC_E_quizz<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Links externos</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_E_link<%= i %>" class="form-label  fs-5">Link externo
                                <%= i + 1 %>
                            </label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][externalLink][<%= i %>]"
                                id="PreC_E_link<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.essential.externalLink[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][essential][externalLink][<%= i %>]"
                                id="PreC_E_link<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carousel1"
                        data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carousel1"
                        data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                </div>
            </div>
            <div class="col-12 col-lg-6">
                <div id="carousel2" class="carousel slide mb-3 carousel-block-1" data-ride="carousel"
                    data-bs-interval="false">
                    <div class="carousel-inner px-5 py-3 bg-light rounded">
                        <h3 class="text-dark text-center text-decoration-underline">Materiais não essenciais</h3>
                        <div class="carousel-item active px-5">
                            <h4 class="">Vídeos</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="video<%= i %>" class="form-label  fs-5">Vídeo <%= i + 1 %></label>
                            <select class="form-select mb-1" aria-label="Select video"
                                name="thisClass[preClassMaterial][nonEssential][video][<%= i %>]"
                                id="PreC_NE_video<%= i %>">
                                <% if (dashboard.classes[classNum]) { %>
                                <% if (dashboard.classes[classNum].preClassMaterial.nonEssential.video[i]) { %>
                                <option value="">Nenhum</option>
                                <% for (let video of videos) { %>
                                <% if
                                                    (video.videoId===dashboard.classes[classNum].preClassMaterial.nonEssential.video[i]){
                                                    %>
                                <option selected value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } else { %>
                                <option value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } %>
                                <% } %>
                                <% } else { %>
                                <option selected value="">Nenhum</option>
                                <% for (let video of videos) { %>
                                <option value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } %>
                                <% } %>
                                <% } else { %>
                                <option selected value="">Nenhum
                                </option>
                                <% for (let video of videos) { %>
                                <option value="<%= video.videoId %>">
                                    <%= video.title %>
                                </option>
                                <% } %>
                                <% } %>
                            </select>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Slides</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_NE_slide<%= i %>" class="form-label  fs-5">Slide <%= i + 1 %></label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][slide][<%= i %>]"
                                id="PreC_NE_slide<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.nonEssential.slide[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][slide][<%= i %>]"
                                id="PreC_NE_slide<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Listas de exercícios</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_NE_lista<%= i %>" class="form-label  fs-5">Lista <%= i + 1 %></label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][exerciseList][<%= i %>]"
                                id="PreC_NE_lista<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.essential.exerciseList[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][exerciseList][<%= i %>]"
                                id="PreC_NE_lista<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Leitura</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_NE_leitura<%= i %>" class="form-label  fs-5">Leitura <%= i + 1 %>
                            </label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][reading][<%= i %>]"
                                id="PreC_NE_leitura<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.nonEssential.reading[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][reading][<%= i %>]"
                                id="PreC_NE_leitura<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Quizzes</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="quizz<%= i %>" class="form-label  fs-5">Quizz <%= i + 1 %></label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][quizz][<%= i %>]"
                                id="PreC_NE_quizz<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.nonEssential.quizz[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][quizz][<%= i %>]"
                                id="PreC_NE_quizz<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                        <div class="carousel-item px-5">
                            <h4 class="">Links externos</h4>
                            <% for (let i=0; i < 3; i++) { %>
                            <label for="PreC_NE_link<%= i %>" class="form-label  fs-5">Link externo
                                <%= i + 1 %>
                            </label>
                            <% if (dashboard.classes[classNum]) { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][externalLink][<%= i %>]"
                                id="PreC_NE_link<%= i %>"
                                value="<%= dashboard.classes[classNum].preClassMaterial.nonEssential.externalLink[i] %>">
                            <% } else { %>
                            <input class="form-control mb-1"
                                name="thisClass[preClassMaterial][nonEssential][externalLink][<%= i %>]"
                                id="PreC_NE_link<%= i %>">
                            <% } %>
                            <% } %>
                        </div>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carousel2"
                        data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carousel2"
                        data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>